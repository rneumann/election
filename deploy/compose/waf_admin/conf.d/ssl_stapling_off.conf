# OCSP Stapling ist bei Self-signed Zertifikaten nicht möglich
# (kein Aussteller-Zertifikat verfügbar). Für produktive CA-Zertifikate
# diese Datei entfernen und ssl_stapling on; in der Haupt-Config aktivieren.
ssl_stapling off;
