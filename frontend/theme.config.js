// Resolved at build time by vite.config.js via `define`.
// At runtime this is a plain object — no file I/O in the browser.
// To change the theme: edit config/theme.<profile>.json and rebuild.
export const themeConfig = __THEME_CONFIG__;
