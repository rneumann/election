# Admin-Frontend Dockerized starten

## Anleitung Dockerized Setup

```text
- In ./admin-frontend das Admin Frontend-Image basierend auf dem Dockerfile bauen:
    - docker build --build-arg NODE_ENV=production -t admin_frontend_image .
```
