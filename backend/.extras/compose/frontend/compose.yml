services:
  frontend:
    image: frontend:latest
    pull_policy: never
    container_name: frontend
    expose:
      - '8080'
    networks:
      - backend_net
      - frontend_net
    depends_on:
      - backend
